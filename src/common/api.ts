export const enum User {
  Login = "/login/ep",
  Register = "/register/user",
  UserInfo = "/user/info",
}

export const enum Question {
  Add = "/questions/add",
  Query = "/questions",
  Submit = "/submit/add",
  QuerySubmit = "/submit",
}
